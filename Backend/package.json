{
  "name": "exploding-kitten-backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "build:ui": "(if exist rmdir build /s /q) && cd ../Frontend/exploding-kittens && npm run build && xcopy build ..\\..\\Backend\\build /s /e /y /i",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "redis": "^3.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.7"
  }
}
